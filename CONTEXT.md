# 领域上下文

## 日志与审计

- **访问日志**：描述一次协议请求的技术事实，例如入口、方法、状态码和耗时；不代表业务动作已经发生。
- **登录审计**：描述身份认证尝试及其结果，例如登录、退出、刷新令牌和多因素认证；关注主体身份与认证方式。
- **业务操作审计**：描述操作者对业务资源发起的意图和结果，例如新增、修改、删除、发布和撤回。
- **数据访问审计**：描述对数据资源的读取、导出、下载或敏感字段访问；关注数据范围和影响行数。
- **权限审计**：描述授权决策或权限配置变更，例如允许、拒绝、角色变更和菜单/API 权限变更。
- **策略评估记录**：描述授权引擎执行策略的输入摘要、命中策略、结果和耗时；它是权限审计的高吞吐技术细分。
- **审计事件**：一条不可变的事实记录，必须能关联租户、主体、请求链路和发生时间；不同审计事实不共用同一张业务表。
- **审计采集管道**：从协议、认证、授权和业务执行过程中提取审计事件，并负责异步传递；它不决定具体审计表的业务字段。
- **审计事实存储**：按审计事实类型分别保存不可变事件的持久化边界；它不把不同事实重新合并成通用日志表。

## 领域边界

- 访问日志回答“请求发生了什么”。
- 登录审计回答“谁尝试以什么方式认证”。
- 业务操作审计回答“谁对哪个业务对象做了什么”。
- 数据访问审计回答“谁接触了哪些数据”。
- 权限审计回答“为什么允许或拒绝”。
- 策略评估记录回答“授权引擎如何得出结论”。
