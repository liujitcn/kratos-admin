# 应用端口。固定端口，避免因 taro-app 占用默认端口后自动漂移，导致局域网访问地址不一致。
VITE_APP_PORT=5004
# H5 根路径
VITE_APP_BASE_PATH=/

# API地址
VITE_APP_BASE_API=/api

# 接口地址
VITE_APP_API_URL=http://localhost:7001

# 静态资源地址
VITE_APP_STATIC_API=

# 静态资源代理地址
VITE_APP_STATIC_URL=http://localhost:7001
