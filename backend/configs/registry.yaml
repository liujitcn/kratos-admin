registry:
  type: "consul"

  consul:
    address: "127.0.0.1:8500"
    scheme: "http"
    token: ""
    # Admin 包含 scheduler 等非 TCP Endpoint，由注册器 TTL 心跳统一维护服务状态。
    health_check: false

  zookeeper:
    endpoints:
      - "localhost:8500"
    timeout: 10s
