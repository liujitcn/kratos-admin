# 代码生成语言目录

`catalog.json` 保存代码生成器写入菜单标题、页面提示、密码强度和静态选项时使用的语言模板。它与后端错误目录、三个前端 workspace 的语言包共同组成完整语言包集合；新增语言必须在这里增加同名数据，并执行仓库根目录的 `make i18n-sync` 校验。

该目录只保存翻译数据，不包含语言判断或业务分支。代码生成器通过 `core/pkg/locale` 的 manifest 遍历全部受支持语言，数据库只负责运行期启用语言和主语言配置。
