初始化默认租户、部门、岗位、字典、配置、菜单、角色和 `super`、`admin` 开发账号。

同时提供管理端 System 页面、AI、代码生成、API 文档、项目文档及应用端公共能力所需的菜单和服务方法权限。项目文档在构建期收集并嵌入 Backend 二进制，项目标识与对应 OpenAPI/Swagger 文档 key 保持一致；OAuth 自动注册与租户编号显示使用布尔配置；OpenAPI 接口元数据和 Casbin 策略在服务启动时按当前文档与菜单关系同步。
